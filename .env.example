# This is an example file. Copy this to a file named .env and fill in your actual values.
# Do NOT commit the .env file to version control.

# A long, random string used for cryptographic signing.
SECRET_KEY='your-secret-key-goes-here'

# API key for the Kavenegar SMS service (optional for now).
KAVENEGAR_API_KEY=''
